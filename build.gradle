plugins {
  id "org.sonarqube" version "3.3"
}

sonarqube {
  properties {
    property "sonar.projectKey", "elision-stages_stage-KDG-BE"
    property "sonar.organization", "elision-stages"
    property "sonar.host.url", "https://sonarcloud.io"
  }
}
